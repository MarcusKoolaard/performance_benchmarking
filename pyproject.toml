[project]
name = "llm-benchmark-app"
dynamic = ["version"]
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.119.0",
    "pydantic-settings>=2.11.0",
    "uvicorn>=0.37.0",
    "databricks-sdk>=0.81.0",
    "aiohttp>=3.9.0",
    "matplotlib>=3.8.0",
    "reportlab>=4.0.0",
    "pillow>=10.0.0",
    "sse-starlette>=2.0.0",
    "psycopg[binary,pool]>=3.1.0",
]

[dependency-groups]
dev = [
    "ty>=0.0.12", "apx==0.2.7",
]

[tool.apx.metadata]
app-name = "llm-benchmark-app"
app-slug = "llm_benchmark_app"
app-entrypoint = "llm_benchmark_app.backend.app:app"
api-prefix = "/api"
metadata-path = "src/llm_benchmark_app/_metadata.py"

[tool.apx.ui]
root = "src/llm_benchmark_app/ui"

[tool.apx.ui.registries]
"@animate-ui" = "https://animate-ui.com/r/{name}.json"
"@ai-elements" = "https://registry.ai-sdk.dev/{name}.json"
"@svgl" = "https://svgl.app/r/{name}.json"


[build-system]
requires = ["hatchling", "uv-dynamic-versioning>=0.7.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "uv-dynamic-versioning"

[tool.uv-dynamic-versioning]
vcs = "git"
fallback-version = "0.0.0"

[tool.uv]

[[tool.uv.index]]
name = "apx-index"
url = "https://databricks-solutions.github.io/apx/simple"

[tool.uv.sources]

[tool.uv.sources.apx]
index = "apx-index"

[tool.hatch.build.hooks.version]
path = "src/llm_benchmark_app/_version.py"
template = '''
version = "{version}"
'''

[tool.hatch.build]
artifacts = ["src/llm_benchmark_app/__dist__", "src/llm_benchmark_app/_metadata.py"]
exclude = ["src/llm_benchmark_app/ui"]
